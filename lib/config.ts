// Feature flags — flip these to enable/disable platform features.
// PAID_CONTESTS_ENABLED: when false, all contests appear free in the UI,
// wallet/deposit/withdraw UI is hidden, and compliance geo-checks are skipped.
export const PAID_CONTESTS_ENABLED = false;
